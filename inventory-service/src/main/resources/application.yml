spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/inventory_db
    username: postgres
    password: QS1YR8URQ6

  jpa:
    hibernate:
      ddl-auto: validate # update
    show-sql: true